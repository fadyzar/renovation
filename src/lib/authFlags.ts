// Feature flags for auth. Google sign-in renders only when this is true AND the
// Supabase Google provider is configured. Flip to false to instantly hide the
// buttons everywhere (e.g. if the Google OAuth app is unpublished).
export const GOOGLE_AUTH_ENABLED = true;
